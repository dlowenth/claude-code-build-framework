# claude-code-build-framework
A battle-tested build framework for Claude Code. Turn a rough idea into a production-quality application with enforced security, phased execution, and lessons learned baked into every build. Includes a governing build contract, PRD template, and kickoff automation prompt.
